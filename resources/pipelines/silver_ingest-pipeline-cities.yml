resources:
  pipelines:
    silver_ingest_pipeline_cities:
      name: silver-ingest-pipeline-cities.yml
      libraries:
        - file:
            path: ../../src/transformations/silver_table_cities.py
      schema: bronze
      catalog: data_catalog
      continuous: false
      development: true # false for production , true for developement
      serverless: true

# in databricks.yml
targets: 
  dev:
    default: true
  prod:
    mode: production
