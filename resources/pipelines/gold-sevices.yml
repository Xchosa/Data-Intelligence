
resources:
  pipelines:
    gold_services:
      name: gold-services.yml
      libraries:
        - file:
            path: ../../src/gold/gold_service_by_country.py
        #- depends_on:
        #    - task_key: silver_departures # Or whatever your silver task is named
        #    - task_key: silver_airports
        #    - task_key: silver_countries
      schema: gold
      catalog: data_catalog
      continuous: false
      development: true
      serverless: true

# in databricks.yml
targets: 
  dev:
    default: true
  prod:
    mode: production
