resources:
  pipelines:
    bronze_pipeline_flight_departures:
      name: bronze--pipeline-departures.yml
      libraries:
        - file:
            path: ../../src/extract_data/bronze_table_flight_departures.py
      schema: bronze
      catalog: data_catalog
      continuous: false
      development: true # false for production , true for developement
      serverless: true

# in databricks.yml
targets: 
  dev:
    default: true
  prod:
    mode: production
