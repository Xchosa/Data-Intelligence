resources:
  pipelines:
    bronze_ingest_pipeline_flight_departures:
      name: bronze-ingest-pipeline-departures.yml
      libraries:
        - file:
            path: ../../src/transformations/bronze_table_resources.py
      schema: bronze
      catalog: data_catalog
      continuous: false
      development: true # false for production , true for developement
      serverless: true

# in databricks.yml
targets: 
  dev:
    default: true
  prod:
    mode: production
