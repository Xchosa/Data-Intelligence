resources:
  pipelines:
    gold_ingest_pipeline_TEST:
      name: gold-ingest-pipeline-test
      libraries:
        - file:
            path: ../../src/analytics_ready/gold_tables_delays.py
      schema: gold
      catalog: data_catalog
      continuous: false
      development: true
      serverless: true

# in databricks.yml
targets: 
  dev:
    default: true
  prod:
    mode: production
