 resources:
   pipelines:
     silver_ingest_pipeline-departure:
       name: silver-ingest-pipeline-departure.yml
       libraries:
         - file:
             path: ../../src/transformations/silver_tables_departure.py
       schema: silver
       catalog: data_catalog
       continuous: false
       development: true # false for production , true for developement
       serverless: true

 # in databricks.yml
 targets: 
   dev:
     default: true
   prod:
     mode: production
